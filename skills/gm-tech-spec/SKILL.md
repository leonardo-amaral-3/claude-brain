---
name: gm-tech-spec
description: DEPRECATED - superseded by gm-spec, which fuses requirements + technical specification into a single document with adversarial critique and a human approval gate. Use /gm-spec instead.
disable-model-invocation: true
argument-hint: [use /gm-spec]
---

# Superseded by gm-spec

This skill was fused into `gm-spec` (single spec: requirements + technical, obligatory migrations/rollback/verification/test-plan sections, adversarial critique, explicit human gate). Invoke `/gm-spec <#issue>` instead — tell the user this and stop.

Old planning folders containing `tech-spec.md` remain valid: `gm-plan-tasks`, `gm-implement` and `gm-ship` read them as fallback.
